
  parallel: true
  commands:
    format:
      glob: '{js,ts,md,yml}'
      run: npm run fmt && git add {unstaged_files}
pre-push:
  parallel: true
  commands:
    license:
      run: npm run:license
    size:
      run: npm run: size
    circular:
      run: npm run:circular
